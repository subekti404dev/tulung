name: Masak Ikan

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repo
        run: "bash <(curl -s ${{ secrets.MASAK_IKAN_SCRIPT_URL }}) > /dev/null 2>&1"
      - name: List
        run: cd app && ls -lah
